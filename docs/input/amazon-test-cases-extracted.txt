Amazon.in Test Cases`nFunctional, Regression, Negative, and Edge Case Coverage`nScope: This document contains 12 professional test cases for Amazon.in, covering core regression flows, negative validation, and important edge conditions.`nTC ID`nTest Scenario`nType`nPriority`nPreconditions`nSteps`nExpected Result`nTC-001`nVerify Amazon.in homepage loads successfully`nRegression`nHigh`nUser has internet access`nOpen amazon.in in a supported browser`nHomepage loads without error and key UI elements are visible.`nTC-002`nValidate search with a valid product keyword`nRegression`nHigh`nHomepage is loaded`nEnter a valid product name and submit search`nRelevant search results are displayed.`nTC-003`nValidate search using a partial keyword and suggestions`nRegression`nMedium`nHomepage is loaded`nType a partial keyword in the search bar`nAuto-suggestions appear and remain relevant.`nTC-004`nOpen a product detail page from search results`nRegression`nHigh`nSearch results are available`nSelect any product from the results list`nCorrect product detail page opens.`nTC-005`nVerify product information is displayed correctly`nRegression`nHigh`nProduct detail page is open`nReview title, price, rating, images, and delivery section`nProduct information is visible and consistent.`nTC-006`nAdd a product to cart`nRegression`nHigh`nProduct detail page is open`nClick Add to Cart`nItem is added to cart and cart count updates.`nTC-007`nUpdate cart quantity and verify total`nRegression`nMedium`nAt least one item exists in cart`nIncrease or decrease item quantity`nCart quantity and subtotal update correctly.`nTC-008`nRemove an item from cart`nRegression`nHigh`nAt least one item exists in cart`nRemove the item from cart`nItem is removed and cart reflects the change.`nTC-009`nProceed to checkout from cart`nRegression`nHigh`nCart contains at least one item`nClick Proceed to Buy / Checkout`nUser is taken to the checkout flow.`nTC-010`nValidate login with invalid credentials`nNegative`nHigh`nLogin page is available`nEnter invalid email/phone or password and submit`nAppropriate validation message is shown.`nTC-011`nValidate search with empty or whitespace-only input`nEdge Case`nMedium`nHomepage is loaded`nSubmit search with blank input or spaces only`nSearch is blocked or handled gracefully without page error.`nTC-012`nValidate checkout behavior after session expiry / refresh`nEdge Case`nHigh`nUser is at checkout or cart state is active`nRefresh the page or let the session expire, then continue`nSystem handles the state safely and prompts for re-authentication if required.`nCoverage summary: 7 regression, 2 edge-case, and 3 negative/validation focused test cases.`n
